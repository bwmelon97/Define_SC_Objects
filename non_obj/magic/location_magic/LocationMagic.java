package non_obj.magic.location_magic;

import non_obj.magic.Magic;

public class LocationMagic extends Magic {
    
    public LocationMagic(final int ENERGY_COST) {
        super(ENERGY_COST);
    }
}